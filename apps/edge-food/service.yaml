apiVersion: v1
kind: Service
metadata:
  name: edge-food-service
spec:
  selector:
    app: edge-food
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 31637
  type: NodePort
